<div class="contain-to-grid">
  <nav class="#d32f2f red darken-2" data-topbar>
    <section class="top-bar-section">
      <ul class="center">
        <!-- link to home -->
        <li>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</li>
        <li><%= link_to "Home", home_path %></li>
        <% if !logged_in? || logged_in? %>
          <li><%= link_to "View Menu", items_path %></li>
        <% end %>
        <% if logged_in? && (current_user.role?(:admin) || current_user.role?(:customer)) %>
          <!-- options for orders -->
          <li class="has-dropdown">
            <%= link_to "Orders", orders_path %>
            <ul class="dropdown">
              <li><%= link_to "My Orders", orders_path %></li>
              <li><%= link_to "New Order", new_order_path %></li>
            </ul>
          </li>

          <!-- options for addresses -->
          <li class="has-dropdown">
            <%= link_to "Addresses", addresses_path %>
            <ul class="dropdown">
              <li><%= link_to "My Addresses", addresses_path %></li>
              <li><%= link_to "Add an Address", new_address_path %></li>
            </ul>
          </li>
        <% end %>

        <li><% if logged_in? %>
          <%= link_to 'Logout', logout_path %>
          <br>[<%= current_user.username %>: <%= current_user.role %>]
          <% else %>
            <li class="has-dropdown">
              <%= link_to 'Login', login_path %>
              <ul class="dropdown"><%= link_to "Register", signup_path %></ul>
          <% end %></li>
        <!-- ADMIN ONLY OPTIONS -->   



    </section>
  </nav>
</div>