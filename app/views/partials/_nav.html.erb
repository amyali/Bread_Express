<nav class="#d32f2f red darken-2">
    <div class="nav-wrapper">
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        <li><%= link_to "Home", home_path %></li>
        <% if !logged_in? || logged_in? %>
          <li><%= link_to "View Menu", items_path %></li>
        <% end %>
        <% if logged_in?%>
          <% if current_user.role?(:admin) || current_user.role?(:customer) %>
            <li><%= link_to "Orders", orders_path %></a></li>
            <li><%= link_to "Addresses", addresses_path%></a></li>
          <% end %>
           <% if current_user.role?(:admin) %>
            <li><%= link_to "Customers", customers_path%></a></li>
          <% end %>
        <% end %>
        <li><a class="dropdown-button" href="#!" data-activates="dropdown1">
          <% if logged_in? %>
            <%= current_user.username %>
          <% else %>
            <%= "Login" %>
          <% end %><i class="mdi-navigation-arrow-drop-down right"></i></a></li>
          <ul id="dropdown1" class="dropdown-content">
            <li><% if logged_in? %>
              <%= link_to "Logout", logout_path %>
            <% else %>
              <%= link_to "Login", login_path %>
            <% end %></li>
            <li><%= link_to "Register", signup_path %></li>
          </ul>
    </ul>
  </div>
</nav>
        
